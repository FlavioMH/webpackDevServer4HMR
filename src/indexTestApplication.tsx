import App from 'app/testApplication/App';
import 'es6-promise/auto';
import * as React from 'react';
import * as ReactDOM from 'react-dom';
import 'whatwg-fetch';

ReactDOM.render(
  <App />,
  document.getElementById('root'),
);
